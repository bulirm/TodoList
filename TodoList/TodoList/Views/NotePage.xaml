<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TodoList.ViewModels"
             x:Class="TodoList.Views.NotePage">
    <ContentPage.BindingContext>
        <viewModels:NoteViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Title"/>
            <Editor Placeholder="Description" Keyboard="Chat" AutoSize="TextChanges"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Margin="10" Text="Cancel" Command="{Binding CancelCommand}"/>
                <Button Grid.Column="1" Margin="10" Text="Save"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>