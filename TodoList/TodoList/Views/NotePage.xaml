<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Title="Create" Icon="baseline_create_white_18dp.png"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TodoList.ViewModels"
             x:Class="TodoList.Views.NotePage">
    <ContentPage.BindingContext>
        <viewModels:NoteViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Resources>
        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="Black"/>
            <Setter Property="TextColor" Value="White"/>
        </Style>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Margin="10">
                <Entry Placeholder="Title" Keyboard="Chat" Text="{Binding Title}"/>
                <Editor Placeholder="Description" Keyboard="Chat" AutoSize="TextChanges" Text="{Binding Description}"/>
            </StackLayout>
            <Grid>
                <Button Margin="10" Text="Save" Command="{Binding SaveCommand}"/>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>